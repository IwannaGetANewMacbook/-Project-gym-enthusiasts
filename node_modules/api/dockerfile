# 1. Node.js 최신 버전 사용
FROM node:18-alpine

# 2. 작업 디렉토리 설정
WORKDIR /app

# 3. package.json 복사 후 의존성 설치
COPY package*.json ./
RUN npm install --only=production

# 4. 모든 코드 복사
COPY . .

# 5. 환경 변수 설정 (Render에서 자동으로 불러옴)
ENV NODE_ENV=production

# 6. NestJS 실행 포트 설정
EXPOSE 3000

# 7. 앱 실행 (환경 변수 로드)
CMD ["npm", "run", "start"]