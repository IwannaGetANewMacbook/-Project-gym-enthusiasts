# 1. 경량화된 Node.js 기반 이미지 사용
FROM node:18-alpine

# 2. 작업 디렉토리 설정
WORKDIR /app

# 3. package.json과 package-lock.json을 복사 후 의존성 설치 (최적화)
COPY package*.json ./

# NestJS 빌드와 실행에는 devDependencies도 필요하므로, 전체 설치
RUN npm install

# 4. 소스 코드 전체 복사
COPY . .

# 5. 환경 변수 설정 (Render에서 자동 불러오기)
ENV NODE_ENV=production

# 6. NestJS 실행 포트 설정
EXPOSE 3000

# 7. NestJS 애플리케이션 빌드 후 실행
RUN npm run build

# 8. 컨테이너 실행 시 start 스크립트 실행
CMD ["npm", "run", "start:prod"]